program BigTest

// =======================


const int MAX = 100;
const char NL = '\';
const bool FLAG = true;

int g1, g2;
int arrGlobal[];

enum Color { RED, GREEN }
enum Light { RED, YELLOW, BLUE = 5, DARK = 8, SEVEN }


// aaaaaaaaaaaaaaaaaaaaaaaaaaaaa
{
void main() 
int a, b, c;
bool d,e,f;
{


    i = 0;
    sum = 0;

    arrLocal = new int[10];



    print(sum);
    print(NL);
}

// ---------- non-void method ----------

int add(int a, int b, int c, int d, int e)
    int result;

	{
    result = a + b;
    return result;
}

// ---------- array parameter ---------- 

int sumArray(int a[], int n)
    int i;
    int s;

  {

    i = 0;
    s = 0;


    return s;
}

// ---------- shadowing test ---------- 

void shadowTest(int g1) 
	int g2;
    int arrGlobal[];
	
	{

    g2 = g1 + 10;
    arrGlobal = new int[5];
    arrGlobal[0] = g2;

    print(arrGlobal[0]);
}

// ---------- const usage ---------- /

int boundedAdd(int x) {
    if (x + 1 > MAX) {
        return MAX;
    }
    return x + 1;
}
}